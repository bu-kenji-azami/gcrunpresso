template:
  taskCount: 2
  parallelism: 1
  template:
    containers:
      - image: "asia-northeast1-docker.pkg.dev/test-project/repo/batch:{{ env "TAG" "v1.0.0" }}"
        args:
          - "--migrate"
    maxRetries: 3
    timeout: "300s"
